apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend-deployment # Debe coincidir con el 
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
  type: ClusterIP
